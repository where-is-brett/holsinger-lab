Person card for the People grid — grayscale 4:5 portrait that colourizes on hover; name and mono role below. Fallback keeps the footprint with mono initials on the striped texture.

```jsx
<PersonCard name="Alan Yan" role="Honours student (Diagnostic Radiography)" img={url} />
<PersonCard name="Jiyoo Choi" role="Undergraduate student" initials="JC" />
```
