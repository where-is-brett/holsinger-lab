The system's single button style — bordered mono caps, square corners, press scale 0.97. Text links handle most actions; reserve Button for real controls (submit, copy, register).

```jsx
<Button onClick={submit}>Send enquiry</Button>
<Button active>✓ Sent</Button>
<Button disabled>Send enquiry</Button>
```
