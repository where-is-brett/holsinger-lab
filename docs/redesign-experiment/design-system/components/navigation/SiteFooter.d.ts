/**
 * Site footer: attribution left, copyright right, mono-caps faint.
 * `compact` stacks the two lines for 390-wide layouts.
 */
export interface SiteFooterProps {
  compact?: boolean;
}
export declare function SiteFooter(props: SiteFooterProps): JSX.Element;
