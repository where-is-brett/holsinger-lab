# Round 1C — lock the merged IA (paste this into the Round 1 session)

Mirror of prompts/round-1c-merge.md in the Claude Design project.
Decision: 1c (output-led) as the spine + 1a's taxonomy as flat tags + a light Research page.
Net: one new type (`resource`), `project` retired, no bios required, nothing that rots.

Key constraint carried in the prompt: agreed-ia.md must describe block ORDER and CONTENT
only — no layout geometry — so Round 2 composition stays open (asymmetry, editorial grids,
off-centre type, full-bleed, varied density).

Correction relayed: 9 publications lack a DOI, not 5 (all 9 have a url fallback).
